# Copy to backend/.env or project root .env

# Unique mandatory LLM (announced Aug 7). Change only this when the tech sheet drops.
LLM_PROVIDER=mock
MODEL_ID=claude-haiku-placeholder
ANTHROPIC_API_KEY=

# Optional: real STT/TTS later. Scaffold uses browser APIs + mock server path.
OPENAI_API_KEY=

# App
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8001
CORS_ORIGINS=http://localhost:5173
DATA_DIR=./data

